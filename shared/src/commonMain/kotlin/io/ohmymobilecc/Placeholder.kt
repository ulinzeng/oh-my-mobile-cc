package io.ohmymobilecc

/**
 * Shared module entry marker. Real logic lives in `core`, `features`, `adapters`, and `ui`
 * packages. Populated starting at W1 (protocol + approval).
 */
public const val SHARED_MODULE_MARKER: String = "oh-my-mobile-cc:shared"

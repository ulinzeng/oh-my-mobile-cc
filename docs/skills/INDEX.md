# Skills Index

Progressive-load budget: total body is ~1368 lines. Load only what matches your task.

| Skill                         | When to load                                           | Size (lines) |
|-------------------------------|--------------------------------------------------------|--------------|
| ansi-parser-deep-dive         | Implementing or debugging an ANSI escape sequence parser for terminal output, including UTF-8 decoding, CJK/emoji width calculation, or state machine design. | 283          |
| compose-canvas-terminal       | Rendering terminal output inside Jetpack Compose or Compose Multiplatform UI, including Canvas drawing, font metrics, scrolling, or fallback to xterm.js. | 324          |
| openspec-workflow             | Writing, editing, validating, or archiving an OpenSpec change proposal, or managing spec lifecycle transitions from proposal to implementation to archive. | 301          |
| stream-json-protocol          | Implementing or debugging a newline-delimited JSON streaming protocol, integrating Claude Code's stream-json I/O, or bridging permission events to mobile Inbox. | 460          |
